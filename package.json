{
  "name": "b-cell",
  "version": "1.1.23",
  "description": "big data",
  "main": "index.js",
  "scripts": {
    "build": "rollup -c",
    "build-uglify": "rollup -c && uglifyjs build/cells.js > build/cells.min.js",
    "build-css": "scss src/themes/cells.scss > build/cells.css",
    "build-all": "npm run build-uglify && npm run build-css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "BigTable",
    "big",
    "data"
  ],
  "author": "koujp",
  "license": "ISC",
  "devDependencies": {
    "rollup": "^0.36.3",
    "rollup-plugin-scss": "^0.1.0",
    "rollup-plugin-uglify": "^1.0.1",
    "ruby": "^0.6.1",
    "scss": "^0.2.4"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/kouyjes/b-cells.git"
  }
}
